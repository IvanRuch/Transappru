import React from 'react';
import { View } from 'react-native';

// Web stub — маршрут pass-yamap.web.tsx перехватывает навигацию на вебе,
// поэтому этот компонент никогда не рендерится. Нужен только чтобы Metro
// не пытался собрать нативный PassYaMapScreen (react-native-yamap-plus).
export default function PassYaMapScreen(_props: any) {
  return <View />;
}
