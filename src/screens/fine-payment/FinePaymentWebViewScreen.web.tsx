import React from 'react';
import { View } from 'react-native';

// Web stub — маршрут fine-payment-webview.web.tsx обрабатывает платежи на вебе через <iframe>.
// Этот компонент нужен только чтобы Metro не пытался собрать нативный
// FinePaymentWebViewScreen (react-native-webview) при веб-сборке.
export default function FinePaymentWebViewScreen(_props: any) {
  return <View />;
}
